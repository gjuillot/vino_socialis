<% provide(:title, @country.name) %>

<p id="notice"><%= notice %></p>

<h1><%= @country.name %></h1>

<% if @regions.any? %>
  <ul>
  	<%= render @regions %>
  </ul>
<% else %>
  No regions yet for this country.
<% end %>

<%= link_to 'Back to all countries', countries_path %>
