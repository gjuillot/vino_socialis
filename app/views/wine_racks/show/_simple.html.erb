
<table>
  <tr>
    <td>
      <%= letter %>
    </td>
    <% (0..(columns - 1)).each do |column| %>
      <td>
      	<%= column %>
      </td>
    <% end %>
  </tr>
  <% (0..(rows - 1)).each do |row| %>
    <tr align="middle">
      <td>
        <%= row %>
      </td>
      <% (0..(columns - 1)).each do |column| %>
        <% position = compartment[row][column] %>
        <%= render :partial => 'wine_racks/show/img', :locals => {:position => position, :move => '0'} %>
      <% end %>
    </tr>
  <% end %>
</table>