<% provide(:title, @region.name) %>

<p id="notice"><%= notice %></p>

<h1><%= @region.name %></h1>

<p>
  <%= link_to @region.country.name, @region.country %>
</p>

<% if @areas.any? %>
  <ul>
  	<%= render @areas %>
  </ul>
<% else %>
  No areas yet for this region.
<% end %>