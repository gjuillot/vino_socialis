<div class="row">
  <div class="span6 offset3">
	<%= form_for(@wine_color) do |f| %>
	  <% if @wine_color.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@wine_color.errors.count, "error") %> prohibited this wine_color from being saved:</h2>
	
	      <ul>
	      <% @wine_color.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>
	
	  <%= f.label :name %><br />
	  <%= f.text_field :name %>

      <%= f.submit class: "btn btn-large btn-primary" %>
	<% end %>
  </div>
</div>