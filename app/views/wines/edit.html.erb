<% provide(:title, t("wine.edit", :wine_name => wine_name(@wine, estate: true, area: true))) %>

<h1><%= t("wine.edit", :wine_name => wine_name(@wine, estate: true, area: true)) %></h1>

<%= render 'form' %>
