<tr>
  <td>
    <%= link_to estate.name, estate %>
  </td>
  <td>
    <%= "#{estate.town} (#{estate.country})" %>
  </td>
  
  <td>
    <% if can? :replace, Estate and @replaced %>
      <%= estate_action_button estate, @replaced %>
    <% else %>
      <%= estate_action_button estate %>
    <% end %>
  </td>
</tr>