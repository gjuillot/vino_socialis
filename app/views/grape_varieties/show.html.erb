<% provide(:title, @grape_variety.name) %>

<h1><%= @grape_variety.name %></h1>

<table class="table table-striped table-condensed">
  <% @wines.each do |wine| %>
    <%= wine_name wine, color: true, estate: true, area:true, html: true, links: true %>
  <% end %>
</table>